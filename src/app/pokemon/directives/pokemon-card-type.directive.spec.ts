import { PokemonCardTypeDirective } from './pokemon-card-type.directive';

describe('PokemonCardTypeDirective', () => {
  it('should create an instance', () => {
    const directive = new PokemonCardTypeDirective();
    expect(directive).toBeTruthy();
  });
});
